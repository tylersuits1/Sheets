{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "create-theme", "export-theme"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
